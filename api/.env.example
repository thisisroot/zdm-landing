# Copy this file to .env on the server (never commit the real .env) and fill in
# the real mailbox credentials for zed@zeddm.ir from your mail provider.
SMTP_HOST=smtp.zoho.com
SMTP_PORT=465
SMTP_USER=zed@zeddm.ir
SMTP_PASS=changeme
CONTACT_TO=zed@zeddm.ir
